{
  "name": "quantum-vault-web-demo",
  "version": "3.1.0",
  "private": true,
  "description": "Quantum Vault — bank vault threshold cryptography demo (AES-256-GCM + Shamir SSS + PBKDF2 + HMAC)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^5.4.0"
  }
}
